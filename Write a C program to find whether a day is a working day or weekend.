#include <stdio.h>

int main() {
    int day;
    printf("Enter day number (1-Mon to 7-Sun): ");
    scanf("%d", &day);

    if (day == 6 || day == 7)
        printf("Weekend");
    else
        printf("Working day");

    return 0;
}
