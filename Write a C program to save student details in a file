#include <stdio.h>

int main()
{
    FILE *fp;
    int id;
    char name[20];

    fp = fopen("student.txt", "w");
    scanf("%d %s", &id, name);
    fprintf(fp, "%d %s", id, name);
    fclose(fp);

    return 0;
}
