#include <stdio.h>

int main() {
    int i;
    float sale, total = 0;

    for (i = 1; i <= 7; i++) {
        printf("Enter sales for day %d: ", i);
        scanf("%f", &sale);
        total += sale;
    }

    printf("Total weekly sales = %.2f", total);
    return 0;
}
