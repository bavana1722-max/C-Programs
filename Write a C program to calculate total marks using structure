#include <stdio.h>

struct marks
{
    int m1, m2, m3;
};

int main()
{
    struct marks m;
    int total;
    scanf("%d %d %d", &m.m1, &m.m2, &m.m3);
    total = m.m1 + m.m2 + m.m3;
    printf("Total Marks = %d", total);
    return 0;
}
