#include <stdio.h>
#include <string.h>

int main() {
    char pass[50];
    printf("Enter password: ");
    scanf("%s", pass);

    if (strlen(pass) >= 6)
        printf("Password is valid");
    else
        printf("Password is invalid");

    return 0;
}
